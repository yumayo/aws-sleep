# 依存パッケージのライフサイクルスクリプトを悪用したサプライチェーン攻撃を防ぐ。
ignore-scripts=true

# 公開から14日を超えたパッケージバージョンのみをインストールする。
min-release-age=14

# 今後追加する依存関係は厳密なバージョンで保存し、ロックファイルを使う。
save-exact=true
package-lock=true

# 有効なTLS証明書を必須にし、インストール時のnpm auditを有効にする。
strict-ssl=true
audit=true
